---
preview: Регулярное выражение состоит из паттерна (он же «шаблон») и необязательных флагов.
```
const regexp = new RegExp("шаблон", "флаги");`
```
или чаще
```
var regexp = /шаблон/; // без флагов
var regexp = /шаблон/gmi; // с флагами gmi (изучим их дальше)
```
Слеши "/" говорят JavaScript о том, что это регулярное выражение. Они играют здесь ту же роль, что и кавычки для обозначения строк.
title: Регулярные выражения
tags: регулярные выражения, regexp, js
author: yemelyanow-ross
date: 2017-12-01 07:12:00
---
Example:
```
const str = "I love JavaScript";
const regexp = /ov/;
console.log( str.search(regexp) ); // 3
```

Метод **str.search(regexp)** возвращает индекс, на котором найдено ПЕРВОЕ совпадение либо -1.

**ФЛАГИ:**
**	i  **  Если этот флаг есть, то регэксп ищет независимо от регистра, то есть не 
	       различает между А и а
**	g ** Если этот флаг есть, то регэксп ищет все совпадения, иначе – только первое.
	** m **  Многострочный режим.
	
метод **str.match(reg)**  *без флага g* - находит первое совпадение и возвращает массив,  состоящий из этого совпадения, с дополнительными свойствами index – позиция, на которой оно обнаружено и input – строка, в которой был поиск.

```
const str = "ОЙ-Ой-ой";

const result = str.match( /ой/i );

console.log( result[0] ); // ОЙ  (совпадение)
console.log( result.index ); // 0 (позиция)
console.log( result.input );
```

*Если часть шаблона обозначена скобками, то она станет отдельным элементом массива.*

```
const str = "javascript - это такой язык";

const result = str.match( /JAVA(SCRIPT)/i );

console.log( result[0] ); // javascript (всё совпадение полностью)
console.log( result[1] ); // script (часть совпадения, соответствующая скобкам)
console.log( result.index ); // 0
console.log( result.input ); // javascript - это такой язык
```